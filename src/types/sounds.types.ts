export type SoundOption = {
  id: number;
  name: string;
  status: string;
  icon: React.ReactNode;
  iconColor: string;
  bgColor: string;
};

export type SoundCategory = "rain" | "nature" | "whiteNoise";
